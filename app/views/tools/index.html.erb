<h1>All Tools</h1>
<%= link_to "Create New Tool", new_tool_path %><br>

<% @tools.each do |tool| %>
  <%= link_to "#{tool.name}", tool_path(tool.id), {class:"hello", class:"another class for css"}%>
  <%= tool.quantity %><br>
<% end %>
