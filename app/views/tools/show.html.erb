<%= link_to "Edit", edit_user_tool_path(@user, @tool) %>
<%= link_to "Delete", user_tool_path(@user, @tool), method: :delete %>

<div class="tool_info">
  <h1> <%= @tool.name %>  - <%= @tool.quantity %></h1>
  <h3> <%= @tool.price  %></h3>
</div>
